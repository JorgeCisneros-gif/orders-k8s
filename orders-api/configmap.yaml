apiVersion: v1
kind: ConfigMap
metadata:
  name: orders-api-config
  namespace: orders
data:
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_NAME: orders_db
  KEYCLOAK_JWK_URI: http://keycloak:8180/realms/orders-realm/protocol/openid-connect/certs
